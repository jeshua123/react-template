const endpoints = {
  //example
  // example: `api/v1/example/`,
  // exampleByfilter: (filters: string) => `api/v1/example?filters=${filters}`,
  // skill: (id: string) => `api/v1/example/${id}/`,
}
export default endpoints
